# Implementation status

## Implemented

- Work order aggregate with guarded lifecycle transitions and SLA-risk calculation
- Application service and repository boundary
- EF Core SQL Server persistence mapping
- ASP.NET Core Minimal API, OpenAPI, CORS, Problem Details and health endpoint
- Responsive Angular operations dashboard with typed models and interactive risk filtering
- Domain unit tests
- Multi-stage API and web containers with local SQL Server composition
- GitHub Actions validation and Dependabot configuration
- Azure Bicep infrastructure baseline

## Roadmap

- Authentication and role-based authorization with Microsoft Entra ID
- EF Core migrations and production seed strategy
- Optimistic concurrency and audit log
- Server-backed dashboard queries and pagination
- Angular component and Playwright end-to-end tests
- Container Apps deployment modules, Key Vault and private endpoints
- OpenTelemetry traces, metrics and operational alerts

Items in the roadmap are intentionally not presented as complete.
